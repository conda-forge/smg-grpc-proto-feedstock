schema_version: 1

context:
  version: "0.4.22"

package:
  name: smg-grpc-proto
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/s/smg-grpc-proto/smg_grpc_proto-${{ version }}.tar.gz
    sha256: 0d69e782cabd4b76480461767351c2acddb887e2fed54bd1e7e4d1d4a52a5bd2
  - url: https://raw.githubusercontent.com/lightseekorg/smg/main/LICENSE
    sha256: be6e05da66141329ef9b092cf9561889baea1aa630576907dda3ec214f8da4eb

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=68.0
    - grpcio-tools >=1.81.1
    - pip
  run:
    - python >=${{ python_min }}
    - grpcio >=1.81.1
    - protobuf >=5.26.0

tests:
  - python:
      python_version:
        - ${{ python_min }}.*
        - '*'
      imports:
        - smg_grpc_proto
      pip_check: true

about:
  summary: SMG gRPC proto definitions for vLLM, TRT-LLM, MLX, TokenSpeed, and SGLang
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/lightseekorg/smg

extra:
  recipe-maintainers:
    - xhochy
